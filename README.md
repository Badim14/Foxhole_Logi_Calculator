# 🦊 Foxhole Logistics Calculator

Веб-приложение для расчета логистики в игре Foxhole.

## 🚀 Быстрый запуск с Docker

### 1. Запуск приложения
```bash
docker-compose up -d
```

### 2. Проверка статуса
```bash
docker-compose ps
```

### 3. Просмотр логов
```bash
docker-compose logs -f app
```

### 4. Остановка
```bash
docker-compose down
```

## 🌐 Доступ к приложению

- **Веб-интерфейс**: http://localhost:3000
- **База данных**: localhost:5432
- **Пользователь БД**: foxhole
- **Пароль БД**: password

## 🔧 Что происходит при первом запуске

1. **Автоматическая инициализация БД** - создаются таблицы
2. **Загрузка данных** - парсинг с вики Foxhole
3. **Скачивание изображений** - иконки предметов и материалов
4. **Запуск веб-сервера** - приложение готово к работе

## 📁 Структура проекта

```
foxhole/
├── docker-compose.yml      # Docker конфигурация
├── Dockerfile             # Образ приложения
├── server/                # Серверная часть
│   ├── index.js          # Основной сервер
│   ├── db.js             # Подключение к БД
│   └── pars.js           # Парсер данных
├── public/                # Фронтенд
└── package.json           # Зависимости
```

## 🐛 Решение проблем

### Пересборка при проблемах
```bash
docker-compose down
docker-compose up --build -d
```

### Полная пересборка
```bash
docker-compose down --rmi all
docker system prune -f
docker-compose up --build -d
```

### Проверка логов
```bash
# Приложение
docker-compose logs app

# База данных
docker-compose logs db
```

## 🎯 Особенности

- ✅ **Автоматическая инициализация** - не нужно запускать парсер вручную
- ✅ **Проверка данных** - парсер запускается только если БД пуста
- ✅ **Обработка ошибок** - детальное логирование проблем
- ✅ **Healthcheck БД** - автоматический контроль состояния

## 📝 Логи

Логи сохраняются в:
- `foxhole_parser.log` - парсер данных
- Docker контейнеры - системные логи
- `./logs/` - папка для логов приложения 